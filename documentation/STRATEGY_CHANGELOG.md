# Strategy Changelog

Last updated: `2026-02-24`

This log captures strategic changes in technical direction, not routine code edits.

## Entries

| ID | Date | Change | Reason | Impact | Rollback Plan | Linked Artifacts |
| --- | --- | --- | --- | --- | --- | --- |
| ST-001 | 2026-02-24 | Introduced a dedicated enterprise-style `documentation/` hub for phase, logic, challenge, and strategy tracking. | Project scope is expanding and ad-hoc documentation no longer scales. | Improves decision traceability and onboarding speed; reduces architecture drift. | Keep existing docs as fallback; remove new docs only if process overhead exceeds value. | `documentation/README.md`, `documentation/PROJECT_PHASES.md`, `documentation/LOGIC_AND_ARCHITECTURE.md` |
| ST-002 | 2026-02-24 | Standardized frontend toggle control in shared JS instead of relying only on vendor defaults. | Inconsistent behavior observed on some pages despite common template usage. | More reliable UI state behavior across route contexts; clearer ownership of interaction logic. | Revert to vendor-only behavior by removing custom handler if conflicts are detected. | `housing_accounting/static/js/project.js`, `documentation/CHALLENGES_AND_DECISIONS.md` |
| ST-003 | 2026-02-24 | Reorganized sidebar navigation into distinct domain tabs and renamed the `Housing` tab to `Society Tab`. | Functional growth required clearer separation by business capability. | Faster navigation, reduced cognitive load, and improved module ownership visibility. | Restore previous sidebar grouping by reverting the navigation block in base template. | `housing_accounting/templates/base.html`, `documentation/CHALLENGES_AND_DECISIONS.md` |
| ST-004 | 2026-02-24 | Converted long flat sidebar into collapsible domain menus for non-daily actions. | Distinct tabs improved structure but increased visible menu length. | Cleaner default sidebar, progressive disclosure of advanced actions, better daily usability. | Revert parent collapse markup and render child links as flat items again. | `housing_accounting/templates/base.html`, `documentation/CHALLENGES_AND_DECISIONS.md` |
| ST-005 | 2026-02-24 | Defined strict parent/caret submenu interaction model. | Need predictable navigation where parent acts as direct shortcut to primary child. | Parent opens first child page directly; submenu expands only on caret; UI remains compact due to auto-collapse. | Restore full-row expansion behavior by re-adding bootstrap collapse triggers to parent links. | `housing_accounting/templates/base.html`, `housing_accounting/static/js/project.js` |
| ST-006 | 2026-02-24 | Standardized action-to-icon mapping for buttons across domain pages. | Mixed/no icon usage made repeated actions harder to scan and slowed operator workflows. | Faster visual recognition and lower UI ambiguity by keeping the same icon for the same action everywhere. | Remove icon prefixes in templates and return to text-only buttons if visual clutter is reported. | `housing/templates/housing/*.html`, `billing/templates/billing/*.html`, `receipts/templates/receipts/*.html`, `notifications/templates/notifications/*.html`, `accounting/templates/accounting/*.html`, `members/templates/members/member_detail.html`, `housing_accounting/templates/users/*.html` |
| ST-007 | 2026-02-24 | Shifted global page subtitle/help text block from top to bottom of content area. | Dense top sections reduced working space and made primary actions less immediately visible. | Cleaner top area for controls and faster first-action workflow; contextual subtitle remains available at page bottom. | Move subtitle block back above messages/content in base layout if users prefer top-context reading. | `housing_accounting/templates/base.html` |
| ST-008 | 2026-02-24 | Standardized common action placement with shared layout classes and button order rules. | Back/save/cancel/common actions were not consistently placed, increasing interaction friction. | Predictable interaction pattern: `Back` stays first in top action rows, and form actions use consistent `Cancel -> Save/Update` placement. | Revert `page-actions` / `form-actions` usage and button ordering if teams want per-page customization later. | `housing_accounting/static/css/project.css`, `housing/templates/housing/*.html`, `billing/templates/billing/*.html`, `receipts/templates/receipts/*.html`, `notifications/templates/notifications/*.html`, `accounting/templates/accounting/*.html`, `members/templates/members/member_detail.html`, `housing_accounting/templates/users/user_form.html` |
| ST-009 | 2026-02-24 | Hardened sidebar submenu toggle logic with a dedicated right-edge caret click zone. | Caret icon hit area was too small, so user clicks were often interpreted as parent-link navigation instead of expand/collapse. | Arrow interaction is now reliable; submenu expands/collapses when clicking caret or its right-side zone, while parent-link navigation behavior remains unchanged. | Revert caret-zone detection and return to strict `.submenu-caret` target matching if unintended toggles are observed. | `housing_accounting/static/js/project.js` |
| ST-010 | 2026-02-24 | Introduced a dedicated `Structure & Units Dashboard` and mapped it as the parent tab landing page. | Parent tab previously routed directly to add-form actions, which is less suitable as a primary module entry. | Parent navigation now lands on a focused dashboard with structure/unit metrics, recent records, and quick actions. | Revert parent href to `structure-add` and remove dashboard route/template if workflow needs to remain form-first. | `housing/views.py`, `housing/urls.py`, `housing/templates/housing/structure_unit_dashboard.html`, `housing_accounting/templates/base.html` |
| ST-011 | 2026-02-24 | Added smart member filtering with structure-first controls and multi-field search. | Member list became harder to use as records grew across multiple structures and units. | Faster member lookup and reduced manual scanning through combined search + structure/role/status filters. | Remove filter UI and queryset conditions to return to plain member listing if simplicity is preferred. | `housing/views.py`, `housing/templates/housing/member_list.html`, `housing/tests/test_views.py` |
| ST-012 | 2026-02-24 | Compacted member search/filter controls into a single-line desktop filter bar. | Multi-row filter layout consumed extra vertical space and slowed scan-to-action flow. | Faster filtering with denser layout; search and filter controls are aligned in one row on desktop while staying responsive on mobile. | Revert to prior grid-based filter layout if users prefer larger vertical spacing. | `housing/templates/housing/member_list.html`, `housing_accounting/static/css/project.css` |
| ST-013 | 2026-02-24 | Polished shared form UX and upgraded receipt posting flow presentation. | Generic form pages lacked consistent visual hierarchy and `/housing/receipts/post/` needed clearer interaction polish. | Cleaner two-column form layout, consistent top/back actions, contextual cancel targets, action-specific submit labels/icons, bootstrap styling for non-model forms, and society-driven reload for receipt posting options. | Revert shared `housing/form.html`, bootstrap form mixin usage, and form UX context fields in views if a minimal legacy form style is preferred. | `housing/templates/housing/form.html`, `housing/views.py`, `housing/forms.py`, `housing_accounting/static/css/project.css`, `housing_accounting/static/js/project.js` |
| ST-014 | 2026-02-24 | Shifted billing charge rules to versioned, effective-date-based templates with immutable historical usage. | Mutable charge templates are unsafe for audit reproducibility and period-level accounting integrity. | Rule changes now happen via new versions; bill generation resolves by period date; bill lines store frozen snapshots (`charge_type`, `rate`, `quantity`, fee basis), and per-sqft logic uses `unit.chargeable_area_sqft`. | If rollout issues occur, keep snapshots but temporarily relax template immutability guard while preserving version fields; do not remove historical versions. | `billing/models/model_ChargeTemplate.py`, `billing/models/model_BillLine.py`, `billing/services.py`, `members/models/model_Unit.py`, `housing/migrations/0005_charge_template_versioning_and_billline_snapshots.py`, `housing/tests/test_phase2_operations.py` |
| ST-015 | 2026-02-24 | Introduced inline charge-template status management in billing list with automatic deactivation date stamping. | Status changes were operationally indirect and could miss consistent `effective_to` values. | Operators can switch status directly from list; inactive transition now auto-sets `effective_to` to current date via server-side rule, keeping timeline consistency. | Revert inline control and endpoint if list-level status edits prove too permissive; keep manual status handling in template edit flow only. | `billing/views.py`, `billing/urls.py`, `billing/templates/billing/charge_template_list.html`, `housing/tests/test_domain_frontend_pages.py` |
| ST-016 | 2026-02-24 | Added one-click `Set Inactive & New` workflow for charge templates with prefilled version creation. | Versioning requires quick handoff from old template closure to new rule setup; separated steps are error-prone in operations. | Action deactivates current version and redirects to prefilled create form (`clone_from`) while linking `previous_version` on save, preserving version-chain continuity. | Roll back to separate manual steps by removing action endpoint/button and keep only independent status change + template add flow. | `billing/views.py`, `billing/urls.py`, `billing/templates/billing/charge_template_list.html`, `housing/views.py`, `housing/tests/test_domain_frontend_pages.py` |
| ST-017 | 2026-02-25 | Standardized local DB switching through `DATABASE_URL` override with preserved localhost fallback. | Temporary external DB checks should not require code edits or risk losing baseline local DB settings. | Developers can test against Supabase (or any Postgres URL) by exporting `DATABASE_URL`, then switch back instantly by unsetting it; rollback complexity is reduced. | Revert `config/settings/local.py` to fixed local dict if env-driven switching causes confusion, but retain runbook documentation. | `config/settings/local.py`, `documentation/DATABASE_SWITCHING.md`, `documentation/README.md` |
| ST-018 | 2026-02-25 | Added conditional `supabase` secondary database alias while keeping local Postgres as primary. | Team needs remote DB migration/testing without replacing local default DB in day-to-day work. | Multi-DB workflow enabled: local remains `default`, Supabase available as `supabase` when env is set; migrations can run independently per alias. | Remove secondary alias block from local settings if single-DB operations are preferred; keep primary fallback unchanged. | `config/settings/local.py`, `documentation/DATABASE_SWITCHING.md` |
| ST-019 | 2026-02-25 | Switched to Supabase-primary with local Postgres retained as explicit read-only secondary alias. | Operational direction changed to run main workflow on Supabase while preserving local DB for safe fallback reads. | `default` now maps to Supabase (env-required), and `local` alias enforces read-only transaction mode to prevent accidental writes. | Revert `config/settings/local.py` to local-primary mapping if offline-first local workflow is reintroduced later. | `config/settings/local.py`, `documentation/DATABASE_SWITCHING.md` |
| ST-020 | 2026-02-25 | Added automatic `.env.local` secret loading for local settings. | Supabase-primary startup required env export every shell and created friction/risk when using inline secrets in commands. | `runserver` and management commands now work consistently when `.env.local` is present; credentials stay outside tracked source with file-level permission control. | Remove `.env.local` autoload block if environment injection is centralized via shell profile or secret manager. | `config/settings/local.py`, `documentation/DATABASE_SWITCHING.md` |
| ST-021 | 2026-03-17 | Realigned local DB code with the documented Supabase-primary topology for testing. | `documentation/DATABASE_SWITCHING.md` described Supabase as `default`, but `config/settings/local.py` still kept localhost as primary, creating drift and inconsistent behavior. | Local Django commands now use Supabase as `default`, fail fast when Supabase env is missing, and preserve localhost as the explicit read-only `local` alias. | Revert `config/settings/local.py` to local-primary mapping if testing should move back to localhost-first behavior. | `config/settings/local.py`, `documentation/DATABASE_SWITCHING.md` |

## When To Add an Entry

Add an entry when a change affects at least one of:

1. Architecture direction.
2. Domain boundaries or ownership.
3. Delivery model or phase priorities.
4. Operational risk posture.
